name: Nightly Desktop

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: nightly-desktop-${{ github.ref }}
  cancel-in-progress: true

env:
  BUN_VERSION: 1.3.3
  NIGHTLY_TAG: nightly
  NIGHTLY_TITLE: CozyBase Nightly
  NIGHTLY_ASSET: CozyBase_nightly_aarch64.app.tar.gz

jobs:
  build-nightly-app:
    name: Build Nightly App Archive
    runs-on: macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build artifacts
        uses: swatinem/rust-cache@v2
        with:
          workspaces: packages/desktop/src-tauri -> target

      - name: Install workspace dependencies
        run: bun install --frozen-lockfile

      - name: Build ad-hoc signed nightly desktop bundle
        run: bun run desktop:build:adhoc

      - name: Normalize nightly app archive
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          shopt -s nullglob
          archive_files=(packages/desktop/src-tauri/target/release/bundle/macos/*.app.tar.gz)

          if [ "${#archive_files[@]}" -ne 1 ]; then
            echo "Expected exactly one app archive, found ${#archive_files[@]}."
            printf '%s\n' "${archive_files[@]}"
            exit 1
          fi

          cp "${archive_files[0]}" "dist/${NIGHTLY_ASSET}"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: cozybase-nightly-app-aarch64
          path: dist/${{ env.NIGHTLY_ASSET }}
          if-no-files-found: error

      - name: Publish nightly release
        env:
          GH_TOKEN: ${{ github.token }}
          ASSET_PATH: dist/${{ env.NIGHTLY_ASSET }}
        run: bash .github/scripts/publish-nightly.sh
