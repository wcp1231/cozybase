{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the CozyBase desktop shell",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
