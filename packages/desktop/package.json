{
  "name": "@cozybase/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx tauri dev",
    "build": "bunx tauri build",
    "prepare:resources": "bun run scripts/build-resources.ts",
    "prepare:sidecar": "bun run scripts/prepare-sidecar.ts",
    "sign:adhoc": "bun run scripts/adhoc-sign.ts",
    "package:nightly": "bun run scripts/adhoc-sign.ts --archive"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
